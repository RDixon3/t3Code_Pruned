# CoCo artwork

`coco/icon.svg` is the source for the desktop and web icons. Run `vp run icons:export` after editing it to regenerate the PNG/ICO and web public assets.

Development and packaged apps use the same CoCo artwork. The macOS launcher creates its ICNS cache from `coco/icon.png`.
